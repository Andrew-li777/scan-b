# 项目问题记录

从项目创建至今遇到的所有问题、根因及解决方案。

> 格式：每条问题记录 **发生时间** 和 **解决时间**，以便追溯问题生命周期。
> 时间戳基于 git commit 日期，~ 表示近似值。

---

## 一、字幕提取

### 1. 两个平台都报「该视频没有可用字幕」  `发生: ~2026-06-01` `解决: ~2026-06-01`

**现象**：粘贴 YouTube 和 Bilibili URL 都提示无字幕。

**根因**：YouTube 的 `except Exception: return []` 静默吞掉所有错误；Bilibili 大部分视频没有 CC 字幕（只有硬字幕）。

**解决**：
- YouTube：改为 3 级回退（youtube-transcript-api → yt-dlp下载SRT → Whisper语音识别），抛出具体错误信息
- Bilibili：双端点尝试 + 明确错误提示，回退 Whisper

**改动文件**：`src/extractors/youtube.py`, `src/extractors/bilibili.py`, `src/transcriber.py`, `src/web/server.py`

---

### 2. youtube-transcript-api v1.x API 变更  `发生: ~2026-06-01` `解决: ~2026-06-01`

**现象**：`YouTubeTranscriptApi.get_transcript()` 报 `no attribute 'get_transcript'`。

**根因**：库从 0.x 升级到 1.x，API 改为实例化后 `list()` + `fetch()`。

**解决**：创建 `api = YouTubeTranscriptApi()` 实例，`api.list(video_id)` 列出语言，`api.fetch(video_id, languages=[...])` 获取。

**改动文件**：`src/extractors/youtube.py`

---

### 3. YouTube 自动生成字幕获取不到  `发生: ~2026-06-01` `解决: ~2026-06-01`

**现象**：某些视频 `api.list()` 返回空或只有少量语言。

**根因**：部分视频上传者禁用了字幕功能（`TranscriptsDisabled`）。

**解决**：列为预期错误，触发 yt-dlp 回退或最终 Whisper 回退。

---

## 二、语音识别 (Whisper)

### 4. ffmpeg 未安装导致音频下载失败  `发生: ~2026-06-01` `解决: ~2026-06-01`

**现象**：`yt_dlp.utils.PostProcessingError: ffprobe and ffmpeg not found`

**根因**：下载配置中指定了 `FFmpegExtractAudio` 后处理器，但 Windows 未装 ffmpeg。

**解决**：去掉后处理器，直接下载原生音频格式（m4a/webm），faster-whisper 用内置 av 库解码。格式优先级：`bestaudio[ext=m4a]/bestaudio[ext=webm]/bestaudio/best`。

**改动文件**：`src/transcriber.py`

---

### 5. Path.rmdir() missing_ok 报错  `发生: ~2026-06-01` `解决: ~2026-06-01`

**现象**：`TypeError: Path.rmdir() got an unexpected keyword argument 'missing_ok'`

**根因**：Python 版本兼容性问题。

**解决**：用 `try/except OSError` 替代 `missing_ok=True`。

**改动文件**：`src/transcriber.py`

---

### 6. CPU 语音识别太慢  `发生: ~2026-06-01` `解决: ~2026-06-02`

**现象**：medium 模型 26 分钟视频转录需 ~25 分钟。

**根因**：模型太大，纯 CPU int8 推理。

**解决**：逐步优化：
1. 模型 medium → small → tiny（精度换速度）
2. `beam_size=5 → 1`（贪心解码）
3. `vad_filter=True`（跳过静音段）
4. 最终：GPU 自动检测 `small + cuda + float16`，CPU 回退 `tiny + int8`

**改动文件**：`src/transcriber.py`

---

### 7. GPU 检测到但无法使用 (cublas64_12.dll)  `发生: ~2026-06-01` `解决: ~2026-06-01`

**现象**：`RuntimeError: Library cublas64_12.dll is not found or cannot be loaded`

**根因**：CUDA Toolkit 13.3 系统中的 cuBLAS DLL 版本不匹配，CTranslate2 需要 CUDA 12 的 `cublas64_12.dll`。`nvidia-cublas-cu12` pip 包已安装该 DLL 到 site-packages，但 Python 的 `os.add_dll_directory()` 对 C 扩展模块无效。

**解决**：在模块顶层（`import faster_whisper` 之前）用 `os.environ["PATH"]` 注入 nvidia 各包的 bin 目录。同时检测系统 CUDA Toolkit 路径。

**改动文件**：`src/transcriber.py` — `_inject_cuda_path()` 函数

---

### 8. int8_float16 在 CPU 上不支持  `发生: ~2026-06-01` `解决: ~2026-06-01`

**现象**：`ValueError: Requested int8_float16 compute type, but the target device or backend do not support efficient int8_float16 computation`

**根因**：混合精度仅特定 CPU 支持。

**解决**：`_get_model()` 中按 `("int8_float16", "int8")` 顺序尝试，失败自动降级。

**改动文件**：`src/transcriber.py`

---

### 9. temperature=0.0 导致转录循环重复  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：英文视频被识别成"好,所以我們現在在這裡..."循环 14 次。

**根因**：`temperature=0.0` + `beam_size=1` 使 softmax 分布过于尖锐，Decoder 陷入循环。

**解决**：去掉 `temperature=0.0`，保留 `num_workers=2` 和 `word_timestamps=False` 两个安全优化。

**改动文件**：`src/transcriber.py`

---

## 三、LLM 调用

### 10. DeepSeek 返回 ThinkingBlock 报错  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：`AttributeError: 'ThinkingBlock' object has no attribute 'text'`

**根因**：DeepSeek 的 Anthropic 兼容端点返回 type="thinking" 的 content block，不是 text block。

**解决**：遍历 `r.content`，跳过非 text 类型，只取 `type == "text"` 的块。

**改动文件**：`src/llm.py`

---

### 11. 12 小时长视频解析失败  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：长视频无响应或超时。

**根因**：Map-Reduce 固定只切 4 块，每块超大；块摘要 token 只有 1024；无进度反馈；SSE 超时 300s。

**解决**：
- 动态分块：~15K 字/块，最多 12 块
- >6 块时双层 Map-Reduce
- 块摘要提升至 2048 token
- SSE 超时延长至 900s
- 新增进度回调推送分段进度

**改动文件**：`src/analyzers/summarizer.py`, `src/web/server.py`

---

## 四、格式化与编码

### 12. Unknown format code    'd' for object of type 'float'

**现象**：`f"{4.0:02d}"` 崩溃。

**根因**：yt-dlp 返回的 `duration` 是 float，`divmod(float, 60)` 返回 `(15.0, 4.0)` 浮点数，`:02d` 要求 int。

**解决**：所有 `divmod(x, 60)` 前加 `int()`；extractor 中 `duration=int(info.get("duration", 0) or 0)`。

**改动文件**：`src/output/markdown.py`, `src/extractors/youtube.py`, `src/extractors/bilibili.py`, `src/web/server.py`

---

### 13. ChromaDB 集合名非法字符  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：`Validation error: name: Expected a name containing 3-512 characters from [a-zA-Z0-9._-], starting and ending with a character in [a-zA-Z0-9]`

**根因**：URL 片段含 `?`、`=` 等字符，`_sanitize_task_id` 未确保首尾为字母数字。

**解决**：`strip("_.-")` 去除首尾非法字符 → 首字符非字母则补 `v_` → 过短则 MD5 hash。

**改动文件**：`src/web/server.py`

---

### 14. Emoji 文件名导致迁移脚本遗漏  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：`🚀国产MiniMax...` 文件夹中 `keypoints.md` 已归类，但 `overview.md` 和 `subtitles.md` 留在 output/ 根目录。

**根因**：旧迁移脚本的正则匹配失败（emoji 前缀）。

**解决**：手动 `mv` 剩余文件，后续新解析直接使用子文件夹结构。

---

## 五、UI

### 15. 进度文本逐行追加导致页面过长  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：Whisper 转录阶段每秒一条进度推送，26 分钟视频堆积 350+ 行文本。

**根因**：前端 `prog.innerHTML += ...` 逐行追加。

**解决**：SSE 事件类型化（`status`/`download`/`transcribe`），前端改为固定 3 行布局（状态文本 + 下载进度条 + 转录进度条）。

**改动文件**：`src/web/server.py`, `src/web/static/index.html`

---

### 16. 无主题切换  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：只有深色模式。

**解决**：CSS 自定义属性 + `[data-theme="light"]` 覆盖 + `localStorage` 持久化 + 右上角 🌙/☀️ 切换按钮。

**改动文件**：`src/web/static/index.html`

---

### 17. 历史记录不显示封面  `发生: ~2026-06-02` `解决: ~2026-06-03`

**现象**：实时解析有封面缩略图，点击历史记录没有。

**根因**：封面 URL 仅通过 SSE 推送（内存），`.md` 文件不存储。历史记录回读 `.md` 文件拿不到封面。

**解决**：分析完成后额外写 `meta.json`（含 title/platform/author/duration/url/thumbnail），历史详情 API 读取该文件。

**改动文件**：`src/web/server.py`, `src/web/static/index.html`

---

### 18. 历史记录排序不对  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：侧栏按文件夹名字母倒序，不是按时间。

**根因**：`sorted(iterdir(), reverse=True)` 按名字排序。

**解决**：按 `overview.md` 的 `st_mtime` 倒序排序。

**改动文件**：`src/web/server.py`

---

### 19. 侧栏收起时按钮位置太近  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：收起后开关按钮紧贴页面标题。

**解决**：`#sidebar.collapsed + #sidebarToggle { left: 0; top: 60px; }`

**改动文件**：`src/web/static/index.html`

---

## 六、输出

### 20. output/ 文件平铺  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：所有视频的 `.md` 文件混在 output/ 根目录。

**解决**：每视频创建子文件夹 `output/{视频名}/`，含 `subtitles.md` / `overview.md` / `keypoints.md` / `meta.json`。下载接口改为 `{filepath:path}` 支持子目录路由。

**改动文件**：`src/output/markdown.py`, `src/web/server.py`, `src/web/static/index.html`

---

### 21. 繁体字幕问题  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：Whisper 转录偶尔输出繁体中文。

**解决**：cleaner 中加 `opencc.OpenCC("t2s").convert(text)`。

**改动文件**：`src/preprocess/cleaner.py`, `pyproject.toml`

---

### 22. 外文视频无翻译  `发生: ~2026-06-02` `解决: ~2026-06-02`

**现象**：英文视频的重点标示和字幕全英文，中文用户阅读不便。

**解决**：
- 新增 `src/translate.py`：CJK 检测 (< 30% 触发翻译)
- 分析管道加翻译阶段：keypoints 批量译 / overview 整体译 / subtitles 分批译
- 前端双语渲染：英文上 + 灰色中文小字下

**改动文件**：`src/translate.py` (新增), `src/web/server.py`, `src/web/static/index.html`

---

## 七、运维

### 23. python -m src.web.server 后台启动退出码 127  `发生: 2026-06-03` `解决: 2026-06-04`

**现象**：通过后台进程管理器启动后进程立刻退出。

**根因**：`reload=True` + `WatchFiles` 在 Windows 上 fork 子进程，父进程退出后子进程脱离。

**解决**：用户直接在终端运行 `python -c "import uvicorn; uvicorn.run('src.web.server:app', host='127.0.0.1', port=8787)"` 作为主要启动方式，避免 `-m` 的 reload 副作用。README 同步更新。

**改动文件**：`README.md`

---

### 24. 服务启动报 [WinError 10048] 端口占用  `发生: 2026-06-03` `解决: 2026-06-04`

**现象**：`ERROR: [Errno 10048] error while attempting to bind on address ('127.0.0.1', 8787): 通常每个套接字地址只允许使用一次`

**根因**：上次启动的 Python 进程未退出，还占用 8787 端口。

**解决**：启动前先 `taskkill //F //IM python.exe` 清理残留进程。

**改动文件**：无（README 已更新启动命令含 taskkill 步骤）

---

### 25. Bilibili 412 Precondition Failed（三轮定位）  `发生: 2026-06-03` `解决: 2026-06-04`

**现象**：解析 Bilibili 视频报 `ERROR: [BiliBili] ...: Unable to download webpage: HTTP Error 412: Precondition Failed`

**第一轮 — UA/Cookie/WBI 签名**（未解决）

以为 Bilibili API 全面要求 cookie → 添加 4 个 UA 轮换、412 指数退避重试、WBI 签名、cookie 文件加载、yt-dlp 也传 cookie。

实际测试发现：`httpx` 打 `api.bilibili.com` 的 REST API **一直返回 200**，并非 412 来源。

**改动文件**：`src/extractors/bilibili.py`, `src/config.py`, `src/web/server.py`, `src/transcriber.py`

**第二轮 — extract() 抛异常丢掉元数据**（解决部分问题）

发现视频无 CC 字幕时 `_fetch_subtitles()` 抛 `RuntimeError`，导致 `extract()` 终止并把已拿到的元数据（title/author/duration）全扔了。server.py 的 `except` 降级到 yt-dlp 做元数据提取，yt-dlp 抓 HTML 页面被 412 拦截。

修复：`extract()` 内 `try/except RuntimeError` 捕获字幕失败，返回空字幕但保留完整元数据。server.py 的 yt-dlp 降级路径也加 try/except 兜底。

**改动文件**：`src/extractors/bilibili.py`, `src/web/server.py`

**第三轮 — transcript.py 的 yt-dlp 音频下载**（真正根因）

前两轮后 412 仍出现。逐环节测试确认：

| 步骤 | 端点 | 方式 | 结果 |
|------|------|------|:--:|
| 视频元数据 | `x/web-interface/view` | httpx | 200 OK |
| 字幕信息 | `x/player/v2` | httpx | 200 OK（但无CC字幕）|
| **音频下载** | **`www.bilibili.com/video/...`** | **yt-dlp** | **412** |
| 流媒体 URL | `x/player/playurl` | httpx | 200 OK → DASH 音频流 |

真正根因：yt-dlp 试图抓取 `www.bilibili.com` HTML 页面获取视频信息时被 412，而 Bilibili 的所有 REST API（`api.bilibili.com`）通过 httpx + 浏览器 UA 可以正常访问，且 `playurl` API 直接返回 DASH 音频流 URL。

修复：`transcriber.py` 中 `download_audio()` 检测 Bilibili URL → 调用 `fetch_bilibili_audio_url()` 通过 `playurl` API 获取 DASH 音频直链 → httpx 流式下载（带进度），完全绕过 yt-dlp。

**改动文件**：`src/extractors/bilibili.py`（新增 `_BILI_API_PLAYURL`、`fetch_bilibili_audio_url()`）, `src/transcriber.py`（新增 `_download_bilibili_audio()`）

---

---

### 26. download 端点路径遍历可读取系统任意文件  `发生: 2026-06-02` `解决: 2026-06-05`

**现象**：`/api/download/../../.env` 可读取项目根目录的 `.env` 文件（含 API Key）。

**根因**：`fp = root / filepath` 直接拼接用户输入的 `filepath`，`..` 可逃逸到系统任意目录。同样问题存在于 `delete_history` 和 `history_detail` 端点。

**解决**：
1. `fp.resolve()` 消除 `..` → `str(fp).startswith(str(resolved_root) + os.sep)` 校验是否仍在 root 内
2. 三个端点统一修复

**改动文件**：`src/web/server.py`

---

### 27. st_mtime 被覆盖导致历史记录时间错乱  `发生: ~2026-05-30` `解决: 2026-06-05`

**现象**：5 条历史记录在 Web UI 上全部显示 2026-06-05 01:23，但文件夹内文件的原始日期不同。

**根因**：`history` 端点用 `overview.md` 的 `st_mtime`（文件修改时间）作为时间戳。`st_mtime` 不可靠——某些进程重写了 5 个 `overview.md`，导致 mtime 被覆盖为相同值。分辨率精确到毫秒（.177 → .181 → .183 → .185），符合循环写入特征。

**解决**：
1. 分析完成时在 `meta.json` 中持久化 `created_at` 字段
2. `history` 端点优先读 `meta.created_at`，回退到 `st_mtime`
3. 已对所有现存记录回填 `created_at`（使用 Windows 文件创建时间 `st_ctime`）

**改动文件**：`src/web/server.py`

---

### 28. 缺乏单元测试  `发生: 项目创建起` `解决: 2026-06-05`

**现象**：项目零测试，任何改动依赖手动验证。

**根因**：开发初期未建立测试习惯。

**解决**：新增 51 个测试用例，覆盖 5 个纯函数模块：
- `tests/test_preprocess/test_cleaner.py` — 9 个测试
- `tests/test_preprocess/test_dedup.py` — 12 个测试
- `tests/test_preprocess/test_segmenter.py` — 12 个测试
- `tests/test_output/test_srt.py` — 8 个测试
- `tests/test_extractors/test_base.py` — 6 个测试

**改动文件**：上述 5 个测试文件 + `tests/conftest.py`

---

### 29. Embedding 模型切换需重建 ChromaDB 索引  `发生: 2026-06-05` `解决: 2026-06-05`

**现象**：从 `all-MiniLM-L6-v2(384d)` 切换到 `BAAI/bge-m3(1024d)` 后，RAG 查询报维度不匹配错误。

**根因**：ChromaDB collection 创建时固定了向量维度，新旧模型维度不同（384→1024），查询向量与存储向量不兼容。

**解决**：删除 `.chromadb/` 目录，下次分析时自动重建。

**改动文件**：`src/rag/vectorstore.py`（模型名一行改动）

---

### 30. `summarize()` async 改造需同步修改调用链  `发生: 2026-06-05` `解决: 2026-06-05`

**现象**：将 `summarize()` 改为 `async def` 后，调用处 `await asyncio.to_thread(summarize, ...)` 在 worker 线程中执行 async 函数导致报错。

**根因**：async 函数必须在事件循环中运行，`asyncio.to_thread` 在新线程中执行，两者不兼容。

**解决**：`pipeline.py` 调用处移除 `asyncio.to_thread` 包装，直接 `await summarize(...)`。

**改动文件**：`src/analyzers/summarizer.py`、`src/translate.py`、`src/web/pipeline.py`

---

### 31. 批量解析资源竞争导致 GPU 空闲 CPU 满载  `发生: 2026-06-05` `解决: 2026-06-05`

**现象**：播放列表批量解析时 CPU/内存 100%，GPU 0% 占用。12 个 yt-dlp 下载同时启动，Whisper 全部回退到 CPU。

**根因**：批量启动时所有视频线程同时运行，GPU 显存不足以同时跑多个 medium 模型，全部 fallback 到 CPU int8。

**解决**：
1. 批量解析改为串行执行 — 一次只处理一个视频
2. `_model_lock` 双检锁保证 Whisper 模型只加载一次
3. 每个视频完成后才启动下一个

**改动文件**：`src/web/server.py`、`src/transcriber.py`

---

### 32. 内容归类策略被否决需按 Author_Date 重分组  `发生: 2026-06-09` `解决: 2026-06-09`

**现象**：用户要求撤销按内容领域分类，改为按 `作者_解析日期` 子文件夹归纳。

**根因**：分组策略不明确。用户要求严格的 `同一播放列表 + 同一作者 + 同批次解析` 规则。

**解决**：
1. 固定批量规则：播放列表链接 + 同一 uploader + 同批次 → `作者_日期` 子文件夹
2. 不满足规则的单视频保留在 vault 根目录

**改动文件**：`src/web/server.py`（batch 端点）

---

### 33. 侧栏分组不显示子目录结构  `发生: 2026-06-09` `解决: 2026-06-09`

**现象**：`3Blue1Brown_2026-06-09` 子目录下 28 个视频与根目录视频平铺混合，看不出文件夹层级。

**根因**：前端 `loadHistory()` 只按 `h.path` 分组，`base_name` 中的子目录前缀被丢弃。

**解决**：两级分组结构 `pathGroups[path] = { items, subs }`，嵌套渲染顶级组 → 子目录组 → 条目。

**改动文件**：`src/web/static/index.html`

---

### 34. 子目录内条目冗余显示文件夹标签  `发生: 2026-06-09` `解决: 2026-06-09`

**现象**：子目录组内每条记录都重复显示 `📁 文件夹名`，但子目录组头已显示。

**解决**：子组内记录项渲染移除 `.hi-path` 行，根目录记录保持不变。

**改动文件**：`src/web/static/index.html`

---

### 35. 四色调色板红色观感不佳  `发生: 2026-06-09` `解决: 2026-06-09`

**现象**：用户反馈红色 `#e74c3c` "观感不太好"，要求更换为紫色。

**解决**：`#e74c3c` → `#9b59b6`（紫色）。四色变为：紫/蓝/绿/橙。

**改动文件**：`src/web/static/index.html`（`PATH_COLORS` 数组）

---

### 36. YouTube 英文字幕视频被解析为中文  `发生: 2026-06-10` `解决: 2026-06-10`

**现象**：3Blue1Brown 英文视频（如 `e^(iπ)_in_3.14_minutes`）的 subtitle 被识别为中文，双语对照中的英文原文缺失。

**根因**：YouTube 字幕语言优先级为 `("zh", "en", "ja", "ko")`，而 3B1B 视频有手动英文字幕 + 自动生成中文字幕。`zh` 优先导致选中自动生成中文，且手动英文未被保留。

**解决**：改为 Manual-first 两通扫描——先扫描 manual 字幕层，再扫描 generated 层；每层内优先级改为 `("en", "zh", "ja", "ko")`。yt-dlp 回退语言列表同步调整。

**改动文件**：`src/extractors/youtube.py`

---

### 37. 历史记录 overview 显示原始 markdown 而非渲染后文本  `发生: 2026-06-10` `解决: 2026-06-10`

**现象**：点击历史记录后，"内容概览" Tab 显示 `# 标题`、`---`、`**来源**:` 等原始 markdown 标记。

**根因**：`renderOverview()` 直接用 `esc(text)` 显示原始文件内容，没有 markdown 转 HTML。

**解决**：前端新增 `mdToHtml()` 函数，将 markdown 语法（标题、粗体、代码块、表格、列表、链接）转为 HTML。`renderOverview()` 调用它渲染。

**改动文件**：`src/web/static/index.html`

---

### 38. 历史记录双语字幕丢失中文翻译行  `发生: 2026-06-10` `解决: 2026-06-10`

**现象**：外文视频的历史记录详情中，字幕只显示英文时间戳行，`[0:00] hello`，缺少下一行的中文翻译。

**根因**：`_load_detail()` 的解析逻辑用 `ts_re.match(line)` 只匹配时间戳行，非时间戳行（中文翻译）被丢弃。

**解决**：解析时增加 "peek next non-empty line" 逻辑——如果时间戳行的下一行不是时间戳格式，则合并为翻译内容，用 `\n` 分隔。同时新增 `translated` 字段标记是否为双语。

**改动文件**：`src/web/history.py`

---

### 39. 批量删除前端串行 N 个 DELETE 请求  `发生: 2026-06-10` `解决: 2026-06-10`

**现象**：选中 26 条记录删除时，前端发起 26 个独立 DELETE 请求，浏览器节流 + 速度慢。

**根因**：`deleteSelected()` 用 `for` 循环串行调用 `fetch('/api/history/' + bn, {method:'DELETE'})`。

**解决**：
1. 后端新增 `POST /api/history/batch_delete`，接收 `base_names` 列表，内部循环调用抽取的 `_delete_one()`
2. 前端改为单次 POST，返回 `deleted`/`failed` 明细，统一 confirm 提示数量

**改动文件**：`src/web/history.py`、`src/web/static/index.html`

---

### 40. server.py 缺少 `import json` 导致 batch 端点崩溃  `发生: 2026-06-10` `解决: 2026-06-10`

**现象**：调用 `/api/export/batch` 时后端报 `NameError: name 'json' is not defined`。

**根因**：`server.py` 顶部没有 `import json`，但 `export_batch()` 中使用了 `json.dumps()`。

**解决**：顶部添加 `import json`。

**改动文件**：`src/web/server.py`

---

### 41. pytest `tmp_path` 在 Windows 上报 PermissionError  `发生: 2026-06-10` `解决: 2026-06-10`

**现象**：运行测试时 `tmp_path.mkdir()` 报 `PermissionError: [WinError 5] 拒绝访问`。

**根因**：Windows 上 pytest 的默认 tmp 目录权限问题。

**解决**：测试改用 `tempfile.mkdtemp(prefix="va_test_")` + `shutil.rmtree(d, ignore_errors=True)` 手动清理。

**改动文件**：`tests/test_web/test_batch.py`

---

### 42. RAG 重排序模型首次加载耗时长  `发生: 2026-06-11` `解决: 2026-06-11`

**现象**：首次 RAG 查询时，`BAAI/bge-reranker-v2-m3` CrossEncoder 加载耗时 10-15 秒，用户感觉卡顿。

**根因**：CrossEncoder 模型约 500MB，首次加载需从 HuggingFace 缓存读取并初始化。

**解决**：懒加载单例模式——`_get_reranker()` 首次调用时加载，之后全局复用。模型加载在首次 RAG 查询时触发，不影响分析管道。

**改动文件**：`src/rag/reranker.py`

---

### 43. 思维导图 LLM 输出格式不稳定  `发生: 2026-06-11` `解决: 2026-06-11`

**现象**：LLM 生成的 Mermaid 有时带 ` ```mermaid ` 包裹，有时只有 ` ``` ` 裸代码块，有时直接输出文本。

**根因**：DeepSeek 对不同 prompt 的代码块格式不一致。

**解决**：`_extract_mermaid()` 三级容错提取。前端 `renderMindmap()` 在 Mermaid 渲染失败时 fallback 显示 `<pre>` 原始文本，不丢失数据。

**改动文件**：`src/analyzers/mindmap.py`、`src/web/static/index.html`

---

### 44. 对比分析端点需要校验输入数量  `发生: 2026-06-11` `解决: 2026-06-11`

**现象**：前端未限制时，用户可能选 1 个或 10 个视频调用 `/api/compare`，导致 LLM prompt 不合理。

**根因**：后端端点未做输入校验。

**解决**：后端校验 2-5 个视频，前端按钮在选中 <2 或 >5 时自动禁用并显示 tooltip 提示。

**改动文件**：`src/web/server.py`、`src/web/static/index.html`

---

### 45. B站 list 合集 URL 误走单视频端点  `发生: 2026-06-23` `解决: 2026-06-23`

**现象**：输入 `https://space.bilibili.com/{mid}/lists/{id}?type=season` 后报 `不支持该视频平台`。

**根因**：前端 `analyze()` 只把 YouTube `playlist?list=` 识别为批量入口。B站 list URL 未被识别，走了 `/api/analyze` 单视频端点，而 `BilibiliExtractor.match()` 只支持 `/video/BV...`。

**解决**：
1. 前端增加 `space.bilibili.com/\d+/lists/` 检测，命中后调用 `/api/analyze/playlist`
2. 后端新增 `expand_playlist()` 统一入口，分别委派 YouTube playlist 和 B站 list
3. B站 list 展开复用 yt-dlp `BilibiliCollectionList` extractor，直接拿 BV URL 列表

**改动文件**：`src/web/static/index.html`、`src/extractors/__init__.py`、`src/extractors/bilibili.py`、`src/extractors/youtube.py`

---

### 46. B站 list 批量解析显示完成但无输出  `发生: 2026-06-23` `解决: 2026-06-23`

**现象**：B站 list 批量任务前端显示“批量分析完成”，但左侧历史栏没有新增记录，OUTPUT_DIR 下也没有对应 `作者_日期` 子文件夹。

**根因**：两层问题叠加：
1. B站大多数视频无 CC 字幕，必须走 Whisper；`_download_bilibili_audio()` 获取 playurl 后，CDN 下载阶段最初未携带 Cookie，容易返回 403
2. `run_pipeline()` 内部捕获异常并只向单视频 SSE queue 推送 `error`，batch 编排层无法感知失败，仍然 `done += 1` 并最终推送 `batch_done`

**解决**：
1. B站音频 CDN 下载 headers 中复用 `BILIBILI_COOKIES` 加 Cookie
2. `run_pipeline()` 返回 `True/False` 表示成功/失败
3. `/api/analyze/playlist` 的 batch tracker 增加 `failed` 计数，`batch_progress` / `batch_done` 事件携带失败数量
4. 前端状态行显示 `(N 个失败)`，有失败时用红色提示

**改动文件**：`src/transcriber.py`、`src/web/pipeline.py`、`src/web/server.py`、`src/web/static/index.html`

---

### 47. YouTube 播放列表展开函数误处理 B站 list URL  `发生: 2026-06-23` `解决: 2026-06-23`

**现象**：B站 list 通过 yt-dlp 能展开，但曾被错误拼成 `https://www.youtube.com/watch?v=BV...`。

**根因**：`extract_playlist_urls()` 没有先校验 URL 是否为 YouTube playlist，直接把任意 URL 交给 yt-dlp；yt-dlp 能识别 B站 list，但函数硬编码拼接 YouTube watch URL。

**解决**：`extract_playlist_urls()` 入口先检查 `_YT_PLAYLIST_PATTERN`，非 YouTube playlist 直接返回空列表，让统一入口继续尝试 B站展开。

**改动文件**：`src/extractors/youtube.py`

### 48. web 页面思维导图显示空白（VSCode 可正常预览）  `发生: 2026-07-08` `解决: 2026-07-08`

**现象**：web 页面"思维导图" Tab 显示空白，但同一 `mindmap.md` 文件在 VSCode 中可正常预览。

**根因**：`mermaid.run()` 是异步函数（返回 Promise），但前端 `renderMindmap()` 中 `try-catch` 只能捕获同步异常。当 `mermaid.run()` 渲染失败时，Promise rejection 未被捕获；Mermaid 内部 `runThrowsErrors` 的 `catch` 块静默处理错误（不抛到外层），但 DOM 中的 `<pre>` 元素已被清空，导致页面空白。

**解决**：改用 `mermaid.render()`（同步返回 SVG 字符串），配合 `async/await` 让 `try-catch` 能真正捕获错误。失败时 fallback 显示原始文本。

**改动文件**：`src/web/static/index.html`

---

## 八、内存与性能治理（2026-08 ~ 09）

> 背景：服务器 3.8GB RAM + 2GB swap，scan 常驻服务与 translate 等服务共存。
> 治理目标：单任务峰值 < 安全线（3.05GB），且任务后内存能回落到基线。

### 49. 超长视频转录峰值内存过高（OOM 风险）  `发生: 2026-08-30` `解决: 2026-09-14`

**现象**：2 小时以上的长视频解析时内存持续攀升，接近/触及 OOM；而短视频无此问题。

**根因**：Whisper 转录一次性把整段音频特征载入，音频越长峰值内存越高，与模型大小无关。

**解决**：**长音频分段转录**——按时长阈值切段逐段转录再拼接：
- `whisper_chunk_seconds: int = 600`（超过 10 分钟即切段）
- `whisper_chunk_overlap: float = 2.0`（相邻段重叠 2 秒，防止边界截断丢字）

实测（2h52m 视频）：峰值 mem_used 2.96GB，全程 swap 零增长，46 分钟跑完。

**改动文件**：`src/transcriber.py`, `src/config.py`

---

### 50. RAG 索引常驻内存 ~1.6GB  `发生: 2026-09-14` `解决: 2026-09-14`

**现象**：解析任务完成后，进程常驻内存居高不下（~1.6GB 无法回收）。

**根因**：RAG 流程会构建 bge-m3 向量索引（嵌入模型 + 向量库）并常驻内存；但主流程（字幕 + 转录 + LLM 分析 + 输出）并不依赖检索功能。

**解决**：新增 **RAG 总开关** `rag_enabled: bool = False`：
- `pipeline.py` 的 build_index / index_global 步骤改为 `if settings.rag_enabled:` 条件执行
- 默认关闭，省 ~1.6GB 常驻内存，whisper + LLM 主流程不受影响
- 需要检索时改 True + 重启即可恢复

**改动文件**：`src/config.py`, `src/web/pipeline.py`

---

### 51. faster-whisper 模型任务后不释放  `发生: 2026-09-14` `解决: 2026-09-14`

**现象**：转录任务结束后 RSS 不回落，模型内存长期占用。

**根因**：CTranslate2/Whisper 模型对象被模块级引用持有，GC 不主动释放。

**解决**：`transcribe()` 的 `finally` 块显式释放模型引用；任务后 RSS 回落到 ~0.3GB。

**诊断方法**（供未来排查参考）：
- `grep whisper /proc/<pid>/maps | wc -l` → 0 表示模型已卸载
- `smaps` 看匿名堆 vs 文件映射（区分「真泄漏」与「glibc 保留堆」）
- `py-spy dump` 看线程状态（全 idle 说明非模型工作）

**改动文件**：`src/transcriber.py`

---

### 52. swap 占用累积不回落  `发生: 2026-09-14` `解决: 2026-09-14`

**现象**：多次长任务后 VmSwap 持续累积（如 74MB 不降），即使 RSS 已回落。

**根因**：Linux 不会主动把已换出的页换回（除非再次访问），VmSwap 会「滞留」。

**解决**：重启 scan 服务清零 VmSwap（systemd `scan.service` 自启+自愈，重启无副作用）。日常观察 swap 只增不减属正常现象，非泄漏。

**改动文件**：无（运维操作）

---

## 九、兼容与健壮性（2026-08 ~ 09）

### 53. B站音频下载 412（durl 回退）  `发生: 2026-08-30` `解决: 2026-08-30`

**现象**：部分 B站视频通过 yt-dlp 下载音频报 412 / 风控拦截。

**根因**：yt-dlp 抓 www.bilibili.com HTML 页面触发风控；而 REST API（api.bilibili.com）持浏览器 UA + cookie 可正常访问，`playurl` 直接返回 DASH 音频流直链。

**解决**：
- 完整 WBI 签名（`_MIXIN_KEY_ENC_TAB` 重排表 + mixin key 1 小时缓存）
- **durl 回退**：playurl API 拿 DASH 音频直链 → httpx 流式下载（带进度），绕过 yt-dlp
- 已配 `bilibili_cookies.txt` 解决风控
- 原文件备份为 `bilibili.py.bak`

**注意**：未登录（code=-101）时 nav 仍返回 wbi_img，不能按 code != 0 直接放弃，否则 AI 字幕主链路在无 cookie 时静默失效。

**改动文件**：`src/extractors/bilibili.py`

---

### 54. LLM 返回内容含括号导致 JSON 提取崩溃  `发生: 2026-09-14` `解决: 2026-09-14`

**现象**：重点提取偶发失败，报 `Expecting value` / `Unterminated string`。

**根因**：旧的非贪婪正则 `\[[\s\S]*?\]` 在**内容本身含方括号**时（如「低保[最低生活保障]」）匹配到第一个 `]` 就截断，json.loads 崩溃。

**解决**：改为扫描 JSON 值起始（`[` 或 `{`）+ `raw_decode()`，正确处理字符串内部的嵌套括号。

**改动文件**：`src/analyzers/summarizer.py`

---

### 55. 字幕来源无法区分  `发生: 2026-09-14` `解决: 2026-09-14`

**现象**：同一视频可能来自 CC 字幕 / AI 字幕 / Whisper 转录，输出中无法区分，排查字幕质量问题困难。

**解决**：`VideoMeta` 新增 `subtitle_source` 字段，取值 `"CC字幕(lan)"` / `"AI字幕(lan)"` / `"whisper"` / `""`。

**改动文件**：`src/models.py`, 提取器/转录器写入该字段

---

### 56. 启动/运行触发 HuggingFace 外网下载（离线化）  `发生: 2026-08-14` `解决: 2026-09-14`

**现象**：服务器网络受限，启动或首次运行时访问 HuggingFace 拉模型易超时失败。

**根因**：faster-whisper / 嵌入模型默认从 HF Hub 在线拉取。

**解决**：**离线模型目录**——
- `model_dir: str = "models"`（本地模型根，whisper-{size} / bge-m3 / bge-reranker-v2-m3 子目录）
- `offline_models_required: bool = False`（True = 启动强校验，缺失直接报错退出）
- `transcriber` 中 `_ensure_model_dir()`：模型必须已存在于本地，**绝不触发 HF 下载**，缺失时报明确指引（`python scripts/download_models.py --whisper`）

**改动文件**：`src/config.py`, `src/transcriber.py`, `scripts/`（下载脚本 HF_HOME 本地化）

---

| 类别 | 问题数 |
|------|:--:|
| 字幕提取 | 5 |
| 语音识别 | 7 |
| LLM 调用 | 2 |
| 格式化/编码 | 3 |
| UI | 9 |
| 输出 | 3 |
| 运维 | 3 |
| 安全 | 1 |
| 工程质量 | 5 |
| 批次/并发 | 4 |
| 分组/归类 | 3 |
| RAG/检索 | 1 |
| AI 生成格式 | 2 |
| 内存与性能 | 4 |
| 兼容与健壮性 | 4 |
| **合计** | **56** |
